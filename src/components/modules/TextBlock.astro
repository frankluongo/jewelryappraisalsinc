---
const { Element = "div", className, ...rest } = Astro.props;
---

<Element class:list={["text-block", className]} {...rest}>
  <slot />
</Element>

<style>
  .text-block {
    --max-width: 33rem;

    max-width: var(--max-width);
    width: 100%;
  }
</style>
